extends layout
block content
  .container.page-wrap
    h1.filename
      a(href=base_path)=('/' + file).replace(/\/[^\/]*$/, '/')
      =file.replace(/^.*\//, '')
      span.last-keypress
    pre.editor(tabindex="1")=contents
    pre.exec-container.container
      input.exec(type='text')
